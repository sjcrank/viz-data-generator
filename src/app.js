import React from 'react';

import Header from './header';
import FixedWidthContainer from './fixedWidthContainer';

const App = () => (
    <div>
        <Header/>
        <FixedWidthContainer/>
    </div>
);

export default App;
